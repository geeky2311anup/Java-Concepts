@Target(ElementType.METHOD)// @Target is used to specify where this annotation can be applied
public @interface TargetAnnotation7 {} // This annotation can be applied to methods only
//ElementType can have several values like TYPE, FIELD, METHOD, PARAMETER, CONSTRUCTOR, LOCAL_VARIABLE, ANNOTATION_TYPE, PACKAGE, TYPE_PARAMETER, TYPE_USE
